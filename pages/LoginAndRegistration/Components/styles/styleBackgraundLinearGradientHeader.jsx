import { StyleSheet } from "react-native";

const styleBackgroundLinearGradientHeader = StyleSheet.create({
    sizeHeader: {
        width: '100%',
        height: '40%',
    },

    icons: {
        width: '100%',
        height: '100%',
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
})

export default styleBackgroundLinearGradientHeader