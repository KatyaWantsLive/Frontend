import { StyleSheet } from "react-native";

const styleHeaderLoginBtn = StyleSheet.create ({
    container: {
        justifyContent: 'center',
        width: '100%',
        height: '50%',
        borderWidth: 1,
        borderTopLeftRadius: 30,
        borderBottomLeftRadius: 30,
    }
})

export default styleHeaderLoginBtn