import { StyleSheet } from 'react-native'

const styleHeaderMain = StyleSheet.create ({
    container: {
        width: '100%',
        height: '15%',
        flexDirection: 'row',
    }
})

export default styleHeaderMain